# Do not recurse into the site build tools itself!
/.site/

# Do not publish the notebook contents; it is not useful to spotlight them.
/notebook/

# Readme files are for github.
README*.md

# ".meta" files are for editor's notes, and shouldn't be rendered to the web.
**/*.meta.*

# Ignore dotfiles in general.  If you want working notes files on your localhost and want to make sure they don't get processed as site content, just use a dotfile.
.*
.*/**

# Ignore the legacy content dir, which will exist during the repo consolidation process.
/_legacy/

# Ignore code packaging
/specs/pkg/
